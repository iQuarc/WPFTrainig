<Window x:Class="WPF_Training2.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:cal="http://www.caliburnproject.org">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="346*"/>
            <RowDefinition Height="25*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200px"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Background="LightGray" Grid.RowSpan="2">
            <Button x:Name="OpenEmployeeModule">Employees</Button>
        </StackPanel>
        <ContentControl x:Name="ActiveItem"  Margin="5,5,5,10" Grid.Column="1" Grid.Row="0" />
        <Label x:Name="Message" Grid.Column="1" Grid.Row="1"/>
    </Grid>

</Window>